/*
 * time_bsp.c
 *
 *  Created on: May 13, 2023
 *      Author: Umaar
 */

#include "Ifx_Types.h"
#include "IfxStm.h"
#include "bsp.h"

Ifx_STM *stmSfr = &MODULE_STM1; // STM module instance

// Initialize the STM module
IfxStm_init(stmSfr);

IfxStm_startTimer(stmSfr);

Ifx_TickTime startTime = get(); // Get the start time

// Your code or delay here

Ifx_TickTime elapsedTime = elapsed(startTime); // Get the elapsed time




